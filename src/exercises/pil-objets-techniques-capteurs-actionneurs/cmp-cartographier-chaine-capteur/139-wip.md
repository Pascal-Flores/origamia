# Cadrage - cmp-cartographier-chaine-capteur - CM2

## Gate 0 - Workflow

- Workflow relu : oui
- Date de relecture : 2026-09-07
- Exercices concernés : 139, 140, 141, 237, 238
- Représentations autorisées : schéma du système en PNG montrant les composants dans un même montage et leurs connexions ; deux libellés sont remplacés par `??? A` et `??? B`.
- Représentations interdites : dessin libre, schéma séquentiel en cases, SVG à annoter, Blockly, pseudo-code.
- Décision : différencier le CM2 du CM1 par une tâche à deux maillons manquants dont la position doit être identifiée.

## Attendu retravaillé

**Compléter une chaîne d’information et d’action en identifiant deux éléments manquants et en les associant à leur emplacement dans le système.**

L’élève observe le système complet et doit retrouver deux maillons complémentaires, généralement un capteur et un actionneur, puis déterminer lequel correspond à `??? A` et lequel correspond à `??? B`.

## Forme exacte

- Type : `ddc`
- Statut : `wip`
- Media : `image`, avec `link: contexte`
- Support attendu : `contexte.png` dans le dossier de l’exercice
- Deux éléments du système sont remplacés par `??? A` et `??? B`
- 5 étiquettes textuelles
- 3 catégories : `??? A`, `??? B`, `Éléments inutiles`
- Une seule étiquette correcte dans `??? A`
- Une seule étiquette correcte dans `??? B`
- Les trois distracteurs vont dans `Éléments inutiles`
- 3 essais
- Feedback essai 1 : rappeler que les deux emplacements n’ont pas le même rôle
- Feedback essai 2 : orienter vers la fonction précise de chaque emplacement sans donner les réponses
- Feedback final : nommer les deux éléments et expliquer leur place dans le trajet information → commande → action
- Aucune remise en ordre et aucun paramétrage de programme

## Variantes

- 139 : casier à badge — A = lecteur de badge ; B = serrure électrique
- 140 : station de tri — A = capteur de couleur ; B = servomoteur
- 141 : portail automatique — A = capteur de distance ; B = moteur électrique
- 237 : store automatique — A = capteur de luminosité ; B = moteur électrique
- 238 : chasse d’eau automatique — A = capteur de présence ; B = électrovanne

## Frontières

- `cmp-lire-capteur` : il ne s’agit pas de choisir un bloc de lecture.
- `cmp-configurer-actionneur` : aucun paramètre d’actionneur n’est réglé.
- `cmp-piloter-actionneur-capteur` : aucune valeur n’est injectée directement dans l’actionneur.
- Ici, l’élève reconstitue deux rôles et leur position dans le fonctionnement global du système.

## Validations utilisateur

- Cadrage : principe de schéma système validé ; nécessité d’associer chaque réponse au bon `???` demandée le 2026-09-07
- Variante pilote : à revoir
- Déclinaison : à revoir
- Review finale : non validée
- Passage testing : non validé
