# Réalignement CM2 — régler un paramètre d’actionneur

## Gate 0 - Workflow

- Workflow relu : oui
- Date de relecture : 2026-09-04
- Représentation(s) autorisée(s) pour cette série : projet Vittascience exécutable, un bloc d’actionneur natif déjà relié à `Au démarrage`, valeur provisoire visible dans son champ.
- Représentation(s) interdite(s) : QCU de reconnaissance, blocs inventés dans le Markdown, plusieurs paramètres à corriger, calcul de valeur, ajout ou déplacement de blocs.
- Décision : review et réalignement de 225–226 autorisés à la demande explicite de l’utilisateur, en reprenant la forme établie de 121–123.

## Attendu

- Compétence : `cmp-configurer-actionneur`
- Attendu : `att-configurer-actionneur-cm2`
- Variantes de référence : 121, 122, 123
- Variantes reprises : 225, 226

## Forme de référence

1. Le bon bloc d’actionneur est déjà relié à `Au démarrage`.
2. Une fiche ou le contexte fournit explicitement la valeur attendue.
3. Une seule valeur du bloc est provisoire et doit être modifiée dans l’interface.
4. Aucun bloc n’est ajouté, supprimé ou déplacé.
5. `type: interface`, `media: interface`, `essais: 2`.
6. Le feedback rappelle que seul le paramètre doit changer puis donne la valeur exacte au feedback final.

## Reprise réalisée

- 225 : maquette de passage piéton ; servomoteur sur P2 ; angle provisoire `0°`, angle attendu `90°`.
- 226 : sèche-mains de démonstration ; ventilateur sur P2 ; puissance provisoire `1023`, puissance attendue `600`.
- Les projets `vittascience.py` reprennent les structures de 121 et 123 avec les mêmes types de blocs natifs.
- `rit-verifier-date-peremption`, déjà réutilisé ailleurs, est abandonné au profit de `rit-laver-mains-correctement` pour 226.

## Review

Verdict : **OK**.

- Même geste que 121–123 : modifier réellement une seule valeur dans un bloc déjà connecté.
- Aucun choix de réponse séparé, aucun bloc à déplacer et aucun calcul à effectuer.
- `type: interface`, `media: interface`, `essais: 2`.
- Les exercices restent en `wip`.
- Aucun build, import Vittascience réel ni synchronisation SQLite n’est revendiqué.

## Validations utilisateur

- Cadrage : validé par la demande de réalignement
- Variante pilote : forme 121–123 déjà établie
- Déclinaison : réalisée pour 225–226
- Review finale : à valider
- Passage testing : non validé
