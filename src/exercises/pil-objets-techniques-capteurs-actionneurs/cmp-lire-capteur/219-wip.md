# Réalignement CM2 — choisir et justifier un capteur

## Gate 0 - Workflow

- Workflow relu : oui
- Date de relecture : 2026-09-04
- Représentation(s) autorisée(s) pour cette série : programme Vittascience incomplet, deux capteurs proposés et quatre réponses textuelles, dont deux choix de bloc et deux justifications.
- Représentation(s) interdite(s) : QCU portant seulement sur le nom du capteur, choix parmi plus de deux capteurs, justification absente, condition ou actionneur.
- Décision : review, réalignement, remplacement de la situation 219 et ajout des projets Vittascience de 219–220 autorisés à la demande explicite de l'utilisateur, en reprenant la forme validée de 112–114.

## Attendu

- Compétence : `cmp-lire-capteur`
- Attendu : `att-lire-capteur-cm2`
- Variantes de référence : 112, 113, 114
- Variantes reprises : 219, 220

## Forme de référence

1. Un besoin de mesure explicite.
2. Dans l'interface, un programme incomplet et exactement deux blocs de capteurs candidats.
3. Quatre réponses : deux `Bloc : ...` et deux `Justification : ...`.
4. L'élève sélectionne exactement un bloc et la justification correspondante.
5. `type: qcm`, `essais: 2`.
6. Le feedback aide à comparer la grandeur recherchée à ce que mesure chaque capteur.

## Retour utilisateur du 2026-09-04

- La situation précédente de 219 autour d'un lecteur de carte / portillon de bus est jugée bizarre.
- Les exercices 217–222 doivent posséder de vrais programmes importables dans Vittascience ; une simple description textuelle ne suffit pas.

## Reprise réalisée

- 219 : nouvelle situation `obj-projection-luminosite-salle`. Avant une projection, la classe veut mesurer la quantité de lumière dans la salle. Les deux blocs proposés sont `luminosité` et `niveau sonore du microphone`. Solution = bloc luminosité + justification sur la quantité de lumière.
- 220 : maquette de bus ; le bouton A de la micro:bit représente le bouton « arrêt demandé ». Les deux blocs proposés sont `état du bouton A` et `niveau sonore du microphone`. Solution = lecture du bouton + justification sur l'appui.

## Projets Vittascience

- `219/vittascience.py` : programme incomplet dans `Répéter indéfiniment`, avec `sensors_getLight` et `io_micro_getSoundLevel` comme deux candidats non connectés.
- `220/vittascience.py` : même structure, avec `io_isButtonPressed` (`BUTTON=a`, `STATE=is_`) et `io_micro_getSoundLevel`.
- Le bloc bouton a été vérifié dans les définitions et le générateur officiels Vittascience ; il produit `button_a.is_pressed()`.
- Les deux fichiers ont été validés avant écriture par analyse syntaxique Python et parsing du XML Blockly, puis relus depuis GitHub après création.
- Aucun import manuel dans le site Vittascience n'a été effectué dans cette passe ; `link: N/A` est conservé jusqu'à création des liens publics.

## Review

Verdict : **OK**.

- Même mécanique que 112–114 : deux capteurs, deux justifications, une paire correcte.
- `type: qcm`, solution `1, 3`, `essais: 2` dans les deux variantes.
- 219 repose désormais sur une situation directe et crédible : mesurer la lumière avant une projection.
- Le programme visible dans chaque projet correspond exactement aux deux blocs cités dans l'énoncé.
- Les exercices restent en `wip`.
- Aucun build ni import SQLite n'est revendiqué.

## Validations utilisateur

- Cadrage : validé par la demande de réalignement
- Variante pilote : forme 112–114 déjà établie
- Ancienne situation 219 : rejetée
- Nouvelle situation 219 : réalisée, à valider
- Déclinaison : réalisée pour 219–220
- Ajout des projets : demandé explicitement, réalisé
- Review finale : à valider
- Passage testing : non validé
