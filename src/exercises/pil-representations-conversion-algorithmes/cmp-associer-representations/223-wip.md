# Reprise des variantes CM2 — associer des représentations

## Gate 0 - Workflow

- Workflow relu : oui
- Date de relecture : 2026-09-03
- Représentation(s) autorisée(s) pour cette série : langage naturel, pseudo-code du projet et blocs textuels Blockly pris en charge par le build.
- Représentation(s) interdites : association un-à-un triviale, syntaxe inventée, écriture libre d'une conversion.
- Décision : nouvelle review de difficulté de 223–224 autorisée à la demande explicite de l'utilisateur ; conserver la matrice 76–78 mais rapprocher fortement les trois familles sémantiques.

## Attendu

- Compétence : `cmp-associer-representations`.
- Attendu : `att-associer-representations-cm2`.
- Variantes de référence : 76, 77 et 78 (`testing`).
- Variantes reprises : 223 et 224.

## Forme de référence

Chaque variante comporte :

1. trois instructions de référence appartenant à une même préparation ;
2. les trois instructions sont présentées sous des formes différentes ;
3. six étiquettes, soit deux représentations équivalentes pour chaque instruction ;
4. l'élève regroupe les six versions sous les trois catégories ;
5. les correspondances reposent sur le sens et non sur la copie exacte des mots ;
6. `type: ddc`, `essais: 3`, avec deux feedbacks progressifs puis un feedback final qui explicite les trois familles.

## Retour utilisateur et problème identifié

- 2026-09-03 : l'utilisateur juge 223–224 beaucoup plus simples que leurs variantes de référence.
- Le nombre de catégories et d'étiquettes était correct, mais les trois familles étaient trop éloignées : `trier / suivre / ranger` pour 223 et `film / popcorn / lumière` pour 224.
- Il était possible de réussir presque uniquement en repérant le verbe ou le champ lexical, sans comparer précisément les informations portées par les représentations.

## Nouvelle déclinaison réalisée

### 223 — LEGO

Les trois catégories portent toutes sur les **mêmes quatre briques rouges** :

- les sortir du bac ;
- les poser sur le plateau ;
- les remettre dans le bac.

Les mots `4`, `briques` et `rouges` ne permettent donc plus de classer une étiquette. L'élève doit comparer le sens de l'action et sa destination.

Solution :

- catégorie 1 : `1, 4` ;
- catégorie 2 : `2, 5` ;
- catégorie 3 : `3, 6`.

### 224 — soirée cinéma

Les trois catégories sont des réglages proches :

- volume à `30 %` ;
- luminosité à `30 %` ;
- volume à `50 %`.

Le mot `volume` apparaît dans deux familles et la valeur `30 %` apparaît dans deux familles. Il faut donc conserver simultanément la propriété et le paramètre.

Solution :

- catégorie 1 : `1, 4` ;
- catégorie 2 : `2, 5` ;
- catégorie 3 : `3, 6`.

## Review

Verdict : **OK**.

- Même structure que 76–78 : trois catégories, six étiquettes, deux représentations équivalentes par catégorie.
- Les trois formes d'écriture sont mélangées entre catégories et étiquettes.
- 223 ne peut plus être résolu par un simple repérage de l'objet ou de la quantité, identiques dans les trois familles.
- 224 ne peut plus être résolu par un seul mot-clé : la propriété et la valeur doivent être vérifiées ensemble.
- Les feedbacks du premier essai indiquent la méthode de comparaison sans donner directement les groupes ; le second explicite les trois familles ; le feedback final justifie les correspondances.
- `type: ddc`, `essais: 3` et `statut: wip` sont conservés.
- Aucun build ni import SQLite n'est revendiqué.

## Validations utilisateur

- Cadrage : validé sur la matrice 76–78
- Première déclinaison : non validée, jugée trop simple
- Nouvelle déclinaison : réalisée
- Review finale : à valider par l'utilisateur
- Passage testing : non validé
