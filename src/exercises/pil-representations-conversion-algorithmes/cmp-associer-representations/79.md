---
nom: Choisir les versions qui disent exactement la même chose (Variante 1)
competence: cmp-associer-representations
attendu: att-associer-representations-6e
type: qcm
statut: wip
situation: rit-preparer-parcours-sport
media: N/A
link: N/A
---

# Énoncé

Voici la suite d’actions de départ :

- placer `4` plots au départ ;
- poser une corde entre les deux bancs ;
- installer un panneau `ARRIVÉE`.

Coche toutes les versions qui disent exactement la même chose.

# Réponses

1. Placer `4` plots au départ, poser une corde entre les deux bancs, puis installer un panneau `ARRIVÉE`.
2. ```
event: DÉBUT
move: PLACER (4) plots au départ
move: POSER une corde entre les deux bancs
say: INSTALLER le panneau (ARRIVÉE)
end: FIN
```
3. ```
event: DÉBUT
move: PLACER (3) plots au départ
move: POSER une corde entre les deux bancs
say: INSTALLER le panneau (ARRIVÉE)
end: FIN
```
4. Installer le panneau `ARRIVÉE`, poser une corde entre les deux bancs, puis placer `4` plots au départ.

# Solution

1, 2

# Feedback

Les réponses `1` et `2` gardent les trois actions, les `4` plots et le même ordre. La réponse `3` change le nombre de plots, et la réponse `4` change l’ordre des étapes.
