# Reprise des variantes CM1 — identifier une forme d’écriture

## Gate 0 - Workflow

- Workflow relu : oui
- Date de relecture : 2026-09-04
- Représentation(s) autorisée(s) pour cette série : langage naturel, pseudocode du projet et blocs textuels Blockly pris en charge par le build.
- Représentation(s) interdites : syntaxe inventée, question isolée portant sur un seul extrait, écriture libre d’une représentation.
- Décision : déclinaison et review de 239–240 autorisées à la demande explicite de l’utilisateur, à partir des variantes 100–102 déjà en `testing`.

## Attendu

- Compétence : `cmp-identifier-formes-algorithme`.
- Attendu : `att-identifier-formes-algorithme-cm1`.
- Variantes de référence : 100, 101 et 102.
- Variantes reprises : 239 et 240.

## Forme de référence

Chaque variante comporte :

1. six extraits indépendants ;
2. trois catégories : `Langage naturel`, `Pseudocode`, `Blocs` ;
3. deux extraits de chaque forme ;
4. un classement `ddc` fondé uniquement sur les indices visibles de la représentation ;
5. `essais: 3`, avec deux feedbacks progressifs puis un feedback final explicitant les indices de forme.

## Reprise réalisée

- 239 conserve `rit-ranger-table-repas`, non utilisé ailleurs dans les exercices de `main`.
- 240 conserve `rit-plier-vetements-ranger`, non utilisé ailleurs dans les exercices de `main`.
- Les anciennes questions QCU sur un seul extrait ont été remplacées par six extraits à classer comme dans 100–102.

## Review

Verdict : **OK**.

- 239 et 240 utilisent désormais `type: ddc`, `essais: 3` et trois catégories identiques aux variantes de référence.
- Chaque exercice comporte exactement six extraits : deux en langage naturel, deux en pseudocode et deux en blocs.
- Les feedbacks font d’abord observer les indices de forme, puis explicitent les trois familles sans demander d’interpréter le comportement.
- 239 et 240 restent en `wip`.
- Aucun build ni import SQLite n’est revendiqué.

## Validations utilisateur

- Cadrage : validé par la demande de réalignement
- Variante pilote : forme 100–102 déjà validée
- Déclinaison : autorisée pour 239–240
- Review finale : à valider par l’utilisateur
- Passage testing : non validé
