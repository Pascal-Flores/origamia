# Reprise des variantes 6e — identifier les formes dans un document composite

## Gate 0 - Workflow

- Workflow relu : oui
- Date de relecture : 2026-09-04
- Représentation(s) autorisée(s) pour cette série : langage naturel, pseudocode du projet et blocs textuels Blockly pris en charge par le build.
- Représentation(s) interdites : simple classement direct des extraits, syntaxe inventée, jugement sur le comportement plutôt que sur la forme visible.
- Décision : déclinaison et review de 243–244 autorisées à la demande explicite de l’utilisateur, à partir des variantes 106–108 déjà en `testing`.

## Attendu

- Compétence : `cmp-identifier-formes-algorithme`.
- Attendu : `att-identifier-formes-algorithme-6e`.
- Variantes de référence : 106, 107 et 108.
- Variantes reprises : 243 et 244.

## Forme de référence

Chaque variante comporte :

1. un document composite présentant quatre extraits identifiés A à D ;
2. les trois formes d’écriture sont présentes, avec une forme représentée deux fois ;
3. six affirmations sur la forme des extraits ;
4. plusieurs affirmations correctes à sélectionner ;
5. `type: qcm`, `essais: 3`, avec deux feedbacks progressifs puis un feedback final qui classe explicitement les quatre extraits.

## Reprise réalisée

- 243 conserve `rit-rendre-livre`, non utilisé ailleurs dans les exercices de `main`.
- 244 conserve `rit-reparer-marque-page`, non utilisé ailleurs dans les exercices de `main`.
- Les anciens classements DDC de six extraits ont été remplacés par un document composite de quatre extraits et six affirmations, comme dans 106–108.

## Review

Verdict : **OK**.

- 243 présente deux extraits en langage naturel, un en pseudocode et un en blocs ; six affirmations sont proposées en QCM.
- 244 présente deux pseudocodes, une phrase en langage naturel et un extrait en blocs ; six affirmations sont également proposées.
- Les deux variantes utilisent `type: qcm`, `essais: 3`, avec quatre affirmations correctes sur six, sans demander de juger le sens ou l’efficacité des programmes.
- Les feedbacks font identifier chaque extrait avant de valider les affirmations.
- 243 et 244 restent en `wip`.
- Aucun build ni import SQLite n’est revendiqué.

## Validations utilisateur

- Cadrage : validé par la demande de réalignement
- Variante pilote : forme 106–108 déjà validée
- Déclinaison : autorisée pour 243–244
- Review finale : à valider par l’utilisateur
- Passage testing : non validé
